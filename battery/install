#!/usr/bin/env bash

set -euo pipefail

cd "$(dirname "$0")"

# shellcheck disable=SC1091
source ./lib.sh

MODULE_DEST="$(module_install_path)"

sudo install -Dm644 oneUpPower.ko "$MODULE_DEST"
install_module_autoload
write_modprobe_config
sudo depmod -a
sudo modprobe oneUpPower
sync
